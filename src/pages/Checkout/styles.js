import SPACING from '../../theme/Spacing';

const CONST = {
  LOGO_SIZE: 64
};

module.exports = {
  photos: {
    height: 64,
    width: 64,
    aspectRatio: 1
  },

  mapView: {
    width: '100%',
    aspectRatio: 4 / 3
  }
};
